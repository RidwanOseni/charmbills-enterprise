version: 11
tx:
  ins:
    - ${in_utxo_0}
    - ${funding_utxo}
  outs:
    - 0:
        ticker: ${ticker}
        remaining: ${remaining}
        metadataHash: ${metadataHash}
        scrollPolicy: ${scrollPolicy}
        payPeriodSeconds: ${payPeriodSeconds}
        compensationSats: ${compensationSats}
  coins:
    - amount: 1000
      dest: ${dest_0} # MUST be Hex-encoded
app_public_inputs:
  n/${app_id}/${app_vk}: ~